using System;namespace GroceryListApp {    public class Item {        // variables are capitalized, params are undercase        public Item(string name, long count) {            Name = name;            Count = count;        }        // declare variable and getter/setter at once        public string Name { get; set; }        public long Count { get; set; }        public override string ToString() {            return Name;        }    }}